define("mod_bigbluebuttonbn/recording_formats_autocomplete",["core/form-autocomplete"],function(Autocomplete){const init=function(selector){if(!selector)return;const element=document.querySelector(selector);if(!element)return;return Autocomplete.enhance(selector,!0)};return{init:init}});
